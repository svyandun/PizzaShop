<%= form_for(@crust) do |f| %>
  <% if @crust.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crust.errors.count, "error") %> prohibited this crust from being saved:</h2>

      <ul>
      <% @crust.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
